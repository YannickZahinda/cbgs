<%= form_with(model: elefe) do |form| %>
  <% if elefe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(elefe.errors.count, "error") %> prohibited this elefe from being saved:</h2>

      <ul>
        <% elefe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
