
<div class="communique-popup-icon">
  <%= image_tag"logos/CBGClogo.png", class: 'my-logo-popup-icon' %>
  <h1>Tableau de bord utilisateur</h1>
  <p>Bienvenue Monsieur/Madame : <%= current_user.first_name%></p>
  <div style="background-color: #f2f2f2; padding: 10px; border-radius: 25px; display: flex; align-items: center; width: 40%; margin: 15px auto">
    <div >
      <%= image_tag "logos/user-profile-icon.png", style: " width: 30px; height: 30px; background-color: #ccc; border-radius: 50%; margin-right: 10px;" %>
    </div>
    <h4 style="margin: 0; font-size: 14px; font-weight: bold; color: #333;"><%= current_user.email %></h4>
  </div>
</div>
<div style="margin: 30px auto; width: 60%">

<div style="display:flex; justify-content: center; gap: 20px; margin: 30px">
  <div style="background-color: #184C72; padding: 15px; border-radius: 10px; text-decoration: none; color: white; width: max-content">
    <%= link_to "Page d'accueil", root_path, style: "text-decoration: none; color: white"%>
  </div>
  <div style="background-color: #184C72; padding: 15px; border-radius: 10px; text-decoration: none; color: white; width: max-content">
    <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, style: "text-decoration: none; color: white;"%>
  </div>
</div>

<div>Tous les messages de l'école ou les devoirs seront affichés ci-dessous. Veuillez avoir les soins de vérifier tous les jours</div>
<div class="admin-letter-container">
  <h2 class="admin-letter-heading">Vos Messages:</h2> 
  <% if @letters.present? %>
    <% @letters.each do |letter| %>
      <div class="admin-letter">
        <div class="admin-letter-header">
          <div class="admin-letter-sender">
            <span class="admin-letter-sender-icon"><i class="fas fa-user-circle"></i></span>
            <span class="admin-letter-sender-name"><%= letter.sender.first_name %></span>
          </div>
          <div class="admin-letter-date">
            <em><%= letter.created_at.strftime("%B %d, %Y") %></em>
          </div>
        </div>
        <div class="admin-letter-body">
          <div class="admin-letter-subject"><%= letter.subject %></div>
          <div class="admin-document" style="padding: 15px 15px">
            <% if letter.document.attached? %>
              <%= link_to "Télécharger le fichier", url_for(letter.document)%>
            <%end%>
          </div>
          <div class="admin-letter-message"><%= letter.body %></div>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="admin-letter-body">
      <div class="admin-letter-message">Vous n'avez pas encore reçu de messages de l'administrateur</div>
    </div>
  <% end %>
</div>
