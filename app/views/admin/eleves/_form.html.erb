<div class="signup">

<%= form_with(model: eleve) do |form| %>
  <% if eleve.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(eleve.errors.count, "error") %> prohibited this eleve from being saved:</h2>

      <ul>
        <% eleve.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
</div>