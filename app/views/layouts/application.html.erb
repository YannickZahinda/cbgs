<!DOCTYPE html>
<html>
  <head>
    <title>Cbgs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'admin', "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'dashboard', "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css" />
    <link href="https://use.fontawesome.com/releases/v5.15.3/css/all.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200&display=swap" rel="stylesheet">

</head>

  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>  

    <% if current_page?(admin_home_index_path) || current_page?(admin_teachers_path) || current_page?(admin_parents_path) || current_page?(admin_communiques_path) || current_page?(admin_eleves_path) || current_page?(logged_in_users_admin_home_index_path) %>
      <div style="display: flex; justify-content: center">
        <div class="sidebar-admin">
          <div class="admin-bar">
            <%= render "admin/home/navbar"%>
          </div>
          <%= yield %>  
        </div>
      </div>
    <% elsif current_page?(dashboard_index_path) || current_page?(dashboard_chats_path)  %>
      <div class="dashboardbard">
        <%= render 'shared/dashboardbar'%>
      </div>
      <div style="width: 70%; margin: 15px auto">
        <%= yield %>  
      </div>
    <% else %>
      <%= yield %>
    <% end %>
     


    <script type="text/javascript" src="//code.jquery.com/jquery-migrate-1.2.1.min.js"></script>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
  </body>

  
</html>
